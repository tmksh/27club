# THE 27 CLUB - バックエンド管理システム 実装概要

## 📦 実装内容

イベントカードとキャスト一覧を、管理画面から追加・編集・削除できるバックエンドシステムを実装しました。

---

## ✅ 実装された機能

### 1. データベース管理（Supabase）

#### **イベント管理テーブル (`events`)**
- タイトル（日本語・英語）
- 開始・終了日時
- 説明文・キャッチフレーズ
- 料金情報
- 画像URL
- 表示順序
- 公開/非公開フラグ

#### **キャスト管理テーブル (`casts`)**
- 名前（日本語・英語）
- プロフィール画像URL
- プロフィール説明
- 特徴・好きなお酒
- ショー説明
- SNSリンク（Instagram、Twitter）
- 表示順序
- アクティブフラグ

#### **セキュリティ機能**
- Row Level Security (RLS) 有効化
- 一般ユーザー：読み取りのみ
- 認証済みユーザー：書き込み可能
- 自動更新日時トリガー

---

### 2. ストレージ管理

#### **画像保存用バケット**
- `events` - イベント画像
- `casts` - キャスト画像

#### **アップロード機能**
- ブラウザからの直接アップロード
- プレビュー機能
- 公開URL自動生成

---

### 3. 管理画面UI

#### **ログイン画面 (`/admin/login`)**
- メール・パスワード認証
- エラーハンドリング
- セキュアなセッション管理

#### **ダッシュボード (`/admin/dashboard`)**

##### イベント管理タブ
- 一覧表示（カード形式）
- 新規作成・編集・削除
- ドラッグ&ドロップで並び替え
- 画像アップロード
- プレビュー機能

##### キャスト管理タブ
- 一覧表示（グリッド形式）
- 新規作成・編集・削除
- ドラッグ&ドロップで並び替え
- 画像アップロード
- SNSリンク設定

---

### 4. フロントエンド統合

#### **イベント表示**
- トップページの「Event Schedule」セクション
- APIからリアルタイムでデータ取得
- カレンダー連動
- 動的グラデーション色分け

#### **キャスト表示**
- トップページの3Dカルーセル
- APIからリアルタイムでデータ取得
- 画像・プロフィール自動表示
- フォールバック対応

---

## 🛠 技術スタック

### バックエンド
- **Supabase** - PostgreSQL データベース + 認証 + ストレージ
- **Supabase Auth** - ユーザー認証
- **Supabase Storage** - 画像保存

### フロントエンド
- **React 18** - UIフレームワーク
- **React Router** - ルーティング
- **@supabase/supabase-js** - Supabaseクライアント
- **react-beautiful-dnd** - ドラッグ&ドロップ
- **Tailwind CSS** - スタイリング
- **GSAP** - アニメーション（既存）

---

## 📂 新規作成ファイル

### ライブラリ
```
src/lib/
└── supabase.js               # Supabase APIクライアント
```

### 管理画面
```
src/screens/Admin/
├── AdminLogin.jsx             # ログイン画面
├── AdminDashboard.jsx         # ダッシュボード
├── index.js                   # エクスポート
└── components/
    ├── EventsManager.jsx      # イベント一覧・管理
    ├── EventForm.jsx          # イベント編集フォーム
    ├── CastsManager.jsx       # キャスト一覧・管理
    └── CastForm.jsx           # キャスト編集フォーム
```

### データベース
```
supabase/migrations/
└── 20241114000000_initial_schema.sql  # スキーマ定義
```

### ドキュメント
```
SETUP.md                       # 詳細セットアップガイド
BACKEND_SETUP_JP.md           # 簡易セットアップガイド（日本語）
IMPLEMENTATION_SUMMARY.md     # このファイル
.env.example                  # 環境変数テンプレート
```

---

## 🔄 更新されたファイル

### ルーティング
```
src/App.jsx                    # 管理画面ルート追加
```

### コンポーネント
```
src/components/Frame628/Frame628.jsx
  → イベントデータをpropsで受け取るように拡張

src/screens/Top/sections/Frame/Frame.jsx
  → APIからイベントデータを取得・表示

src/screens/Top/sections/FrameWrapper/FrameWrapper.jsx
  → APIからキャストデータを取得・表示
```

### 設定ファイル
```
.gitignore                     # .env除外を追加
package.json                   # 依存関係追加
```

---

## 🎨 デザインシステム

管理画面は既存のサイトデザインに合わせた配色：

- **背景**: グラデーション（`#0c1e1a` → `#1a2a28`）
- **アクセントカラー**: エメラルドグリーン（`#00d6bd`）
- **カード背景**: 半透明ダークグリーン（`#1a2a2880`）
- **ボーダー**: 半透明エメラルド（`#00d6bd20`）
- **フォント**: システムで使用中のフォントファミリー

---

## 🔒 セキュリティ対策

### 実装済み
- ✅ Row Level Security (RLS)
- ✅ 認証必須の管理画面
- ✅ 公開データは読み取り専用
- ✅ 環境変数による機密情報管理
- ✅ HTTPS通信（Supabase標準）

### 推奨追加対策（本番環境）
- 管理者ロールの細分化
- IPアドレス制限
- ファイルサイズ・形式制限
- レート制限
- 監査ログ

---

## 📊 データフロー

```
┌─────────────────┐
│  管理画面        │
│  /admin/*       │
└────────┬────────┘
         │
         │ (認証済み)
         ↓
┌─────────────────┐
│  Supabase API   │
│  (PostgreSQL)   │
└────────┬────────┘
         │
         │ (公開データ)
         ↓
┌─────────────────┐
│  フロントエンド   │
│  /               │
└─────────────────┘
```

---

## 🚀 使用開始手順

### 1. Supabase設定
```bash
# 1. Supabaseプロジェクト作成
# 2. SQL実行（migrations/xxx.sql）
# 3. Storageバケット作成（events, casts）
# 4. 管理者ユーザー作成
```

### 2. ローカル環境設定
```bash
# .env ファイル作成
cp .env.example .env
# ↑ Supabase URLとキーを設定

# 依存関係インストール
npm install

# 開発サーバー起動
npm run dev
```

### 3. 管理画面アクセス
```
http://localhost:5173/admin/login
```

---

## 🧪 テスト方法

### 管理画面のテスト

1. **ログイン**
   - `/admin/login` にアクセス
   - 作成した管理者アカウントでログイン

2. **イベント管理**
   - 新規イベント作成
   - 画像アップロード
   - 編集・削除
   - ドラッグ&ドロップで並び替え

3. **キャスト管理**
   - 新規キャスト作成
   - 画像アップロード
   - SNSリンク設定
   - 編集・削除
   - ドラッグ&ドロップで並び替え

### フロントエンドのテスト

1. **イベント表示**
   - トップページの「Event Schedule」を確認
   - 管理画面で追加したイベントが表示されるか
   - カレンダーとの連動を確認

2. **キャスト表示**
   - トップページの3Dカルーセルを確認
   - 管理画面で追加したキャストが表示されるか

---

## 📝 今後の拡張案

### 機能追加
- [ ] イベント検索・フィルター
- [ ] キャスト詳細ページ
- [ ] 予約管理システム連携
- [ ] メール通知機能
- [ ] アクセス解析
- [ ] 多言語対応

### 管理機能強化
- [ ] 一括編集
- [ ] CSVインポート/エクスポート
- [ ] 画像編集機能
- [ ] プレビューモード
- [ ] 変更履歴

### パフォーマンス
- [ ] 画像最適化（自動リサイズ）
- [ ] キャッシング戦略
- [ ] CDN連携
- [ ] 遅延ロード

---

## 🐛 既知の制限事項

1. **ドラッグ&ドロップライブラリ**
   - `react-beautiful-dnd`は非推奨ですが、安定動作
   - 将来的に`@dnd-kit/core`への移行を推奨

2. **画像ファイルサイズ**
   - 現在制限なし
   - 本番環境では制限を設定すること

3. **認証**
   - 基本的なメール/パスワード認証のみ
   - 2要素認証未実装

---

## 💡 開発のポイント

### API設計
- `src/lib/supabase.js`に全APIロジックを集約
- エラーハンドリングを統一
- 型安全性を意識したPropTypes定義

### コンポーネント設計
- 管理画面は独立したディレクトリ
- フォームコンポーネントの再利用性
- ローディング状態の統一管理

### データ管理
- Supabaseの自動ID生成を活用
- display_orderで柔軟な並び替え
- is_published/is_activeでソフト削除

---

## 📞 サポート

詳細は以下のドキュメントを参照：
- **セットアップ**: `SETUP.md`
- **クイックスタート**: `BACKEND_SETUP_JP.md`
- **Supabase公式**: https://supabase.com/docs

---

**実装完了日**: 2024年11月14日
**バージョン**: 1.0.0

